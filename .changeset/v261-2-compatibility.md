---
"systemd-ts": minor
---

Confirm compatibility with systemd v261.2 after reviewing the cached v261.1 and v261.2 manpages. No directive updates are required because the only typed unit-file surface change corrects the documented `ExitType=` constraint without changing directive syntax.
